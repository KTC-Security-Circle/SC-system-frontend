import { Button } from "@mui/material";

export default async function UseButton() {
    return <Button variant="contained" color="primary" style={{ marginTop: '1.5em', width: "9em", height: "2.5em", fontSize: "1.05em" }}>使ってみる</Button>
}