import Calendar from "@/Components/Calendar/calendar";
import { Container } from "@mui/material";
import { Box } from "@mui/system";

export default async function ScheduleManagement() {
    return (
        <Calendar />
    )
}